import Foundation
import UIKit

class RemoteRepository {
//    private let networkManager = NetworkManager()
//    private var baseURL = try? URL(string: "http://louie-03.com")
//
//    func getHeroImage(completion: @escaping (String) -> Void) -> Void {
//        guard var url = self.baseURL else { return }
//        url.appendPathComponent("/banners/main")
//
//        let request = URLRequest(url: url)
//
//        self.networkManager.fetchData(request: request, type: HeroImage.self) { result in
//            switch result {
//            case .success(let image):
//                completion(image.imageURL)
//            case .failure(_):
//                break
//            }
//        }
//    }

//    func getPopularLocation(completion: (PopularLocation)) -> Void {
//        guard var url = self.baseURL else { return }
//        url.appendPathComponent("/travels/popular")
//
//        let request = URLRequest(url: url)
//
//        self.networkManager.fetchData(request: request, type: PopularLocationElement.self) { result in
//            switch result {
//            case .success(let location):
//                completion(location)
//            case .failure(let error):
//                print(error)
//            }
//        }
//    }
    

}
